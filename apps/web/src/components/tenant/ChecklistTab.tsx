/** ChecklistTab — placeholder; full implementation lands in Sprint 2 (S2-C). */
"use client";

export function ChecklistTab({ tenantId: _tenantId }: { tenantId: string }) {
  return (
    <p style={{ color: "#7A8499", fontSize: "13px", padding: "12px 0" }}>
      Intake Checklist — built in Sprint 2.
    </p>
  );
}
