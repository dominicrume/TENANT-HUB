/** SessionsTab — placeholder; full implementation lands in Sprint 2 (S2-A). */
"use client";

export function SessionsTab({ tenantId: _tenantId }: { tenantId: string }) {
  return (
    <p style={{ color: "#7A8499", fontSize: "13px", padding: "12px 0" }}>
      Sessions — built in Sprint 2.
    </p>
  );
}
