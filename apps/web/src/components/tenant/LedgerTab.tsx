/** LedgerTab — placeholder; full implementation lands in Sprint 2 (S2-B). */
"use client";

export function LedgerTab({ tenantId: _tenantId }: { tenantId: string }) {
  return (
    <p style={{ color: "#7A8499", fontSize: "13px", padding: "12px 0" }}>
      Service Charge Ledger — built in Sprint 2.
    </p>
  );
}
